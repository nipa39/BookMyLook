<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uzma.bookmylook">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/bml"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProviderPicUpload" />
        <activity android:name=".ImagesActivity" />
        <activity android:name=".Retrieve" />
        <activity android:name=".MyServices" />
        <activity android:name=".ParlourMenu" />
        <activity android:name=".UserLoginPg" />
        <activity android:name=".ProviderLoginPg" />
        <activity android:name=".ProviderProfile" />
        <activity android:name=".Fixit" />
        <activity android:name=".Appointment" />
        <activity android:name=".UserPicUpload" />
        <activity android:name=".SharedMoments" />
        <activity android:name=".EmailNotification" />
        <activity android:name=".EditProfile" />
        <activity android:name=".Rating" />
        <activity android:name=".MyRatings" />
        <activity android:name=".MyProfile" />
        <activity android:name=".UpdateServices" />
        <activity android:name=".GridLayout" />
        <activity android:name=".ClientHistory" />
        <activity android:name=".UserNav" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBYVj8B54cMZi97ZHQAcS64-t-RvyjCjF8" />

        <activity android:name=".MapsActivity" />
        <activity android:name=".MyAddress" />
        <activity android:name=".MyUserProfile" />
        <activity android:name=".MyUserBookings" />
        <activity android:name=".SharePhoto" />
        <activity android:name=".AppInfo"></activity>
    </application>

</manifest>